
<% if @suggestion %>
  <p><%= @suggestion.name %></p>
<% end %>
  
<%= form_for ([@user, @trip, @list]) do |f| %>
  <div class="field">
  	<%= f.label :owner %>
  	<%= f.text_field :owner %>
  </div>

  <div class="actions">
  	<%= f.submit %>
  </div>
<% end %>
